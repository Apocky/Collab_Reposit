{
  "name": "halo-pocket-labyrinth",
  "version": "0.1.0",
  "description": "Mobile-first HALO Pocket Labyrinth build outputs with optional relay server",
  "scripts": {
    "build": "node scripts/build.js",
    "server": "node scripts/server.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.948.0",
    "@aws-sdk/lib-dynamodb": "^3.948.0",
    "ws": "^8.18.0"
  }
}
